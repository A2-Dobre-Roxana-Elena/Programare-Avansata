/**
 * In acest enum se regasesc tipurile de sali existente pentru cerinta data.
 * Salile pot fi de tip laborator de informatica respectiv 'COMPUTER_LAB'
 * cat si sala de lectura respectiv 'LECTURE_HALL'.
 */
public enum RoomType {
    LECTURE_HALL,
    COMPUTER_LAB;
}